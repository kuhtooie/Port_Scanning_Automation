from random import randint
import math
import time
#randint is probably a collection of 1,000 1's and zero's spawned off the flow of electricity in a list, squared.
#thank you sister Peterson. The wholly spirit (theta wave) was very happy with me meeting you.
check_complexity(vars):
    pass
get_complexity(vars):
    pass
def markov_complexity(vars):
    complexity = check_complexity(vars)
    if complexity:
        return {"Noun":vars,get_complexity('noun')}
    else:
        return {"Verb":vars,get_complexity('verb')}
'''
Nouns 50-60%
verbs 25-30%
adjectives 15%
Adverbs 5%
'''
d = {}
e = []
i=1

a = 1
d = {}
for i in range(100):
    e =a*a+1/a+2
    d[i] = markov_complexity([math.sqrt(e),e**2,e])

if time.delta(1):
    OAuth = d[e[randint(0,len(e)-1)]][randint(0,2)]
