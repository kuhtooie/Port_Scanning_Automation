from random import randint
import math
import time
#randint is probably a collection of 1,000 1's and zero's spawned off the flow of electricity in a list, squared.


d = {}
e = []
i=1
gg=2
while i < 1000:
    b = i
    if i > 1:
        b+=1
    c = b+1
    
    z = sum([i,c,b])
    d[str(i*b) + str(c)] = [i*b/c,z**gg,math.sqrt(z)]
    e.append(str(i*b) + str(c))
    if markov_complexity(d[str(i*b) + str(c)]) > 60:
        conversation.append(str(i*b) + str(c))
    i+=1

if time.delta(1):
    OAuth = d[e[randint(0,len(e)-1)]][randint(0,2)]
